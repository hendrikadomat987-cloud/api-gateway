You are an expert backend engineer specializing in n8n workflows and multi-tenant SaaS systems.

Your task is to generate a complete n8n workflow JSON for:

Customer UPDATE

# PROJECT CONTEXT

You are working inside the following directory:

C:\Benutzer\hendr\claude-ai-voice-agent\customer_update

This directory contains:

- _customer_create.json
- _customer_get.json
- _customer_delete.json
- customer_update_spec.md
- customer_update_prompt.txt

# MANDATORY STEPS

1. Read:
   ./customer_update_spec.md

2. Analyze existing workflows:
   - ./_customer_create.json
   - ./_customer_get.json
   - ./_customer_delete.json

3. Reuse patterns, structure, and response formats from these workflows.

IMPORTANT:
- The UPDATE workflow MUST be consistent with CREATE, GET, and DELETE
- Follow the same node structure, naming, and response format

# SECURITY RULES

- tenant_id comes ONLY from JWT (via API Gateway)
- NEVER trust client input
- DO NOT accept tenant_id from user input
- ALL database queries MUST include tenant_id

# ENDPOINT

PUT /api/v1/customer?id=<id>

# INPUT

- tenant_id → $json.body.tenant_id
- id → $json.query.id
- optional:
  - name
  - phone
  - email

# VALIDATION RULES

- tenant_id must exist
- id must exist
- at least ONE of:
  - name
  - phone
  - email

IF validation fails:
→ return VALIDATION_ERROR or MISSING_ID

# WORKFLOW STRUCTURE

Webhook (PUT)
→ Set Fields
→ Validate

→ IF (valid?)

    → FALSE:
        → Respond Error

    → TRUE:
        → Validate Tenant
        → Merge

        → IF (id exists?)

            → FALSE:
                → Respond Error (MISSING_ID)

            → TRUE:
                → DB UPDATE

                → IF (rows affected?)

                    → FALSE:
                        → NOT_FOUND

                    → TRUE:
                        → Format Response
                        → Respond Success

# DATABASE QUERY

UPDATE customers
SET
  name = COALESCE($1, name),
  phone = COALESCE($2, phone),
  email = COALESCE($3, email)
WHERE id = $4
AND tenant_id = $5

Params:
={{ [ $json.name, $json.phone, $json.email, $json.id, $json.tenant_id ] }}

# RESPONSE FORMAT

SUCCESS:
{
  "success": true,
  "data": {
    "id": "...",
    "updated": true
  }
}

ERROR:
{
  "success": false,
  "error": {
    "code": "...",
    "message": "..."
  }
}

# ERROR CODES

- VALIDATION_ERROR
- MISSING_ID
- NOT_FOUND

# OUTPUT

Return ONLY valid n8n workflow JSON.
No explanations.
No comments.
No markdown.
