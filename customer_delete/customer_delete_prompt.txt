You are an expert n8n backend engineer.

Load the specification from:
./customer_delete_spec.md

Generate a complete n8n workflow JSON for Customer DELETE.

Follow all rules strictly:
- Multi-tenant secure (tenant_id required)
- Validate input (tenant_id + id)
- Use DELETE query with tenant_id
- Use proper n8n nodes (Webhook, Set, Validate, IF, Merge, DB, Respond)

Return ONLY valid JSON.
